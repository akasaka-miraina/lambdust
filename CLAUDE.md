# Lambdust 開発ルール

## 開発サイクルの基本原則

### 1. サブエージェント協業体制の徹底

開発タスクにおいて、以下のサブエージェント体制を必須とする：

#### 必須サブエージェント
- **language-processor-architect**: 言語処理システム（パーサ、AST、セマンティクス分析）の設計
- **cs-architect**: アルゴリズム設計、データ構造選択、計算複雑性分析、システムアーキテクチャ決定
- **rust-expert-programmer**: 各architectからの設計をRustで実装するプログラマ
- **lambdust-r7rs-programmer**: LambdustとR7RSに精通したScheme/Lamndustプログラマ

#### 協業体制のルール
1. 複雑な実装タスクを開始する前に、必ず適切なサブエージェントに相談する
2. 設計段階でサブエージェントの専門知識を活用し、実装方針を確定する
3. サブエージェントから提供された設計指針・アーキテクチャに従って実装する
4. 実装完了後、再度サブエージェントによるレビューを実施する

### 2. フェーズ完了時のエラー・警告ゼロ状態

#### 必須チェック項目
各開発フェーズの完了時に以下を必須とする：

```bash
# コンパイルエラーチェック
cargo check --all-targets --all-features

# Clippyによる静的解析
cargo clippy --all-targets --all-features -- -D warnings

# テスト実行
cargo test --all-features

# フォーマットチェック
cargo fmt --check
```

#### エラー・警告対応方針
1. **コンパイルエラー**: 完全に解決するまで次のフェーズに進まない
2. **Clippy警告**: 全て解決またはジャスティファイ（#[allow]による明示的許可）
3. **テスト失敗**: 全テストパス確認後に次フェーズへ
4. **フォーマット違反**: cargo fmtによる自動修正の適用

### 3. 開発フェーズの定義

#### Phase 1: 設計・アーキテクチャ
- サブエージェント協業による設計レビュー必須

#### Phase 2: 基本実装
- 設計に従った基本機能実装
- エラー・警告ゼロ状態での完了

#### Phase 3: テスト・検証
- 包括的テストケース作成・実行
- エラー・警告ゼロ状態での完了

#### Phase 4: 最終統合
- 全体統合テスト
- 一時的に作ったファイルの削除
- エラー・警告ゼロ状態での完了

### 4. 現在の課題（2025年8月時点）

#### 解決すべき主要問題
- 291個のコンパイルエラー
- 2個のClippy警告
- 主要な問題領域：
  - JITセキュリティフレームワークの不足実装
  - 型システムの不整合（String vs Box<String>）
  - パターンマッチングの不完全性
  - SIMD最適化関連の未実装部分

### 5. 品質保証のためのチェックリスト

開発作業完了前に以下を確認：

- [ ] 適切なサブエージェントによる設計レビュー実施済み
- [ ] `cargo check` エラーゼロ
- [ ] `cargo clippy` 警告ゼロ（またはジャスティファイ済み）
- [ ] `cargo test` 全テストパス
- [ ] `cargo fmt --check` フォーマット適合
- [ ] 実装がR7RS Scheme仕様に準拠
- [ ] メモリ安全性の確保
- [ ] パフォーマンス要件の充足
- [ ] 一時的に作ったファイルの削除

### 6. コードベース保守性の維持

#### ファイルサイズ管理
- **最大20,000トークン制限**: 各ソースファイルはClaudeエンジンの性能を最大化するため20,000トークン以下を維持
- **モジュール分割戦略**: 大きなファイルは機能・責任別にモジュール分割
- **リファクタリング実績**:
  - `src/stdlib/lists.rs` (23,000トークン) → 9モジュールに分割（最大3,444トークン）
  - `src/stdlib/strings.rs` (20,600トークン) → 4モジュールに分割（最大980トークン）

#### モジュール設計原則
1. **責任分離**: 各モジュールは単一の責任を持つ
2. **API一貫性**: 外部インターフェースの互換性維持
3. **依存関係管理**: 循環参照を避けた階層構造
4. **再エクスポート**: `mod.rs`による透過的なアクセス提供

#### 品質保証プロセス
- リファクタリング後も必ずエラー・警告ゼロ状態を維持
- 段階的な分割による動作確認
- API互換性の継続的な検証

### 7. 継続的改善

このルールは開発の進行に伴い、必要に応じて更新・改善を行う。
新しい課題や改善点が発見された場合は、このドキュメントを更新し、チーム全体で共有する。

#### 主要な改善実績（2025年8月）
- ✅ **291個のコンパイルエラー**完全解決
- ✅ **150個のClippy警告→0個**完全達成
- ✅ **大容量ファイルのモジュール化**完了
- ✅ **開発効率の大幅向上**実現